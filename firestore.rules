rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Règles temporaires pour le développement (en mode test)
    match /{document=**} {
      allow read, write: if true;
    }
    
    // TODO: Sécuriser avant la production
  }
}
